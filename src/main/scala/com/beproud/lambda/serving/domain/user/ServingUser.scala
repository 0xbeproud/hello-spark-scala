package com.beproud.lambda.serving.domain.user

case class ServingUser(
                        walletAddress: String,
                        nickname: String,
                        isActive: Boolean
                      )
